name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set Version
      run: echo "VERSION=1.2.3" >> $GITHUB_ENV

    - name: Debug Version
      run: echo "Version is $VERSION"
